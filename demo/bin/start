#!/usr/bin/env node

const runner = require('node-shell-runner');
const path = require('path');

runner.run([
    [`npm install`, 'Installing dependencies'],
    [`nodemon ${path.join(__dirname, '..', 'app')}`, 'Starting app with nodemon']
]);
